[tool.poetry]
name = "viikko2"
version = "0.1.0"
description = ""
authors = ["pomiska <miska99@live.fi>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
toml = "^0.10.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
